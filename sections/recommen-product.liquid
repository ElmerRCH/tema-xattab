{% schema %}
{
  "name": "Recommended Products",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"

    },
    {
      "type": "number",
      "id": "limit",
      "label": "Number of products",
      "default": 8
    }
  ]
}
{% endschema %}

{% assign collection = collections[section.settings.collection] %}
{% render 'product-carousel', collection: collection %}
